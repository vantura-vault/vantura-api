# Server Configuration (no quotes needed)
PORT=3000
NODE_ENV=development

# CORS (no quotes needed)
CORS_ORIGIN=http://localhost:5174

# JWT Secret (no quotes needed)
JWT_SECRET=paste-from-render

# Database (no quotes needed - paste directly from Render)
DATABASE_URL=postgresql://user:password@host:5432/dbname

# BrightData API (no quotes needed)
BRIGHTDATA_API_KEY=paste-from-render

# Redis (no quotes needed - paste directly from Render)
REDIS_URL=redis://user:password@host:6379

# OpenAI / LLM (no quotes needed)
LLM_API_KEY=sk-paste-your-openai-key
LLM_API_BASE=https://api.openai.com/v1
LLM_MODEL=gpt-4o-mini

# AWS S3 - optional (no quotes needed)
# AWS_ACCESS_KEY_ID=paste-from-render
# AWS_SECRET_ACCESS_KEY=paste-from-render
# AWS_S3_BUCKET=vantura-profile-images
# AWS_REGION=us-east-1
